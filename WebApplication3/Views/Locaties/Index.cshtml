@model IEnumerable<WebApplication3.Models.Locatie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Vanzare.vanzareId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descriere)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Vanzare.vanzareId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descriere)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.locatieId }) |
            @Html.ActionLink("Details", "Details", new { id=item.locatieId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.locatieId })
        </td>
    </tr>
}

</table>
